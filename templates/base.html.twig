<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}Watson
			{% endblock %}
		</title>
		<link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
		<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.css">
		<link rel="stylesheet" href="{{ asset('styles/app.css') }}"> {% block stylesheets %}{% endblock %}

		{% block javascripts %}
			{% block importmap %}
				{{ importmap('app') }}
			{% endblock %}
		{% endblock %}
	</head>
	<body>
		<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
			<div class="container">
				<a class="navbar-brand fw-bold" href="{{ path('app_home') }}"></a>

				<div class="navbar-nav ms-auto">
					{% if app.user %}
						<div class="navbar-nav">
							<span class="navbar-text me-3">
								Bonjour,
								{{ app.user.username }}
							</span>
							<a class="nav-link" href="{{ path('app_backoffice') }}">
								<i class="bi bi-gear-fill"></i>
								Backoffice
							</a>
							<a class="nav-link" href="{{ path('app_logout') }}">
								<i class="bi bi-box-arrow-right"></i>
								Déconnexion
							</a>
						</div>
					{% else %}
						<div class="navbar-nav">
							<a class="nav-link me-2" href="{{ path('app_login') }}">
								<i class="bi bi-box-arrow-in-right"></i>
								Connexion
							</a>
						</div>
					{% endif %}
				</div>
			</div>
		</nav>

		{% block body %}{% endblock %}

		<footer>
			<div class="container">
				<p>Watson - Réalisé avec amour par le Groupe 6 |
					<a href="{{ path('rss_feed') }}" target="_blank" title="Flux RSS">
						Flux RSS
					</a>
					{% if not app.user %}
						|
						<a href="{{ path('app_register') }}" target="_blank" title="Register">
							S'inscrire
						</a>
					{% endif %}
				</p>
			</div>
		</footer>
	</body>
</html>
